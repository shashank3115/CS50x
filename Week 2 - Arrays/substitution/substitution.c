#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>


char character(void);

int main(int argc,string argv[])
{

    printf("%s\n",argv[1]);
    for(int i = 0 , len = strlen(argv[1]) ; i < len ; i++)
    {
        char charcter = argv[1][i];
    }

    
    for(int i = 0 , len = strlen(argv[1]) ; i < len ; i++)
    {

    }

}



char character(void)
{
    for(char i = 'A';i <= 'Z';i++)
    {
        char character2 = i;
    }
    printf("\n");
return character;
}
